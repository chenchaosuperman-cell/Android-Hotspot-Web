# Android-Hotspot-Web 版本验收标准（VERIFICATION）

> **最终验收原则（置顶，每次发版必须执行）**
>
> **禁止仅做静态代码验收。所有热点、代理、MAC、配置持久化相关项目，必须以真机实际系统状态作为 PASS 标准。**
>
> 任何功能必须同时回答三个问题：
> 1. **代码层**：代码有没有正确执行？
> 2. **UI 层**：页面有没有正确告诉用户发生了什么？
> 3. **系统层**：Android 实际有没有发生对应变化？
>
> 三个答案全部是 YES，该功能才标记 PASS。不能只因为 CGI 返回 `{"ok":true}` 就认为验收完成。

---

## 适用范围

- Xiaomi / Redmi、HyperOS、Android 16、KernelSU
- Android-Hotspot-Web v1.7.x 及后续版本

验收分为：**代码验收 / UI 验收 / 功能验收 / 异常与恢复验收 / 发布验收**

原则：
- 代码检查通过 ≠ 功能通过
- 页面显示正常 ≠ 后端执行正常
- 接口返回成功 ≠ 系统状态真的改变
- 最终必须以**真机实际状态**作为功能是否通过的判断标准

---

## 一、代码验收标准

### 1. Shell 语法
所有 `.sh`、CGI Shell 文件执行 `sh -n 文件名`，要求 **0 个 Syntax Error**。
范围至少：`service.sh action.sh customize.sh uninstall.sh common.sh control.cgi status.cgi traffic.cgi device.cgi proxy.cgi notify.cgi`。
出现任一语法错误 → 本版本代码验收失败。

### 2. JavaScript 语法
所有 JS 执行 `node --check`；JS 写在 index.html 时先提取 `<script>` 内容再检查。
要求 SyntaxError=0、ReferenceError=0。禁止出现：`xxx is not defined` / `Unexpected identifier` / `Unexpected token` / `Cannot read properties of undefined`。
重点检查历史变量：`notifyThresholdInput`、`ym`、`d`。

### 3. HTML 基础检查
标签完整、**无重复 ID**、input 对应 label、button 有明确 type、form 不误提交、script 加载顺序正确、CSS/JS 路径正确。
浏览器控制台 Error=0；Warning 允许但须确认不影响功能。

### 4. 配置字段验收
保存 → 重启服务 → 重新读取，前后完全一致。
覆盖：SSID、PASSWORD、BAND、CHANNEL、HIDDEN、KEEPALIVE、AUTO_OFF、PROXY_SCOPE、PROXY_SELF、MAC_MODE、BLOCKED_MACS、ALLOWED_MACS、通知配置、流量套餐、定时配置。
禁止：页面显示已保存但 config.conf 没写进去；config.conf 正确但页面读取成默认值。

### 5. 配置字段默认值一致性
每个配置字段必须有唯一默认值。禁止前后端不一致（如前端 `PROXY_SCOPE=hotspot`、后端 `both`）。
**前端默认 = common.sh 默认 = save_config 默认 = status 输出默认 = README 描述，完全一致。**

### 6. 配置升级兼容
必须测试 v1.7.2 / v1.7.3 / v1.7.4 → 当前版升级。
要求：SSID、密码、流量配置、通知配置、Mihomo 订阅、设备备注、MAC 名单不丢；
新增字段不存在时自动使用新版本安全默认值；不得导致整个配置文件重置。

### 7. 路径检查
禁止代码残留旧模块路径 `xiaomi14_mifi_web`，只能保留当前正式目录（`xiaomi_mifi_web`）。
`grep -R "xiaomi14_mifi_web" .` 命中时须确认是文档历史内容，运行代码里不得存在。

### 8. 危险命令检查
检查 `rm -rf`、`iptables -F`、`iptables -X`、`pkill`、`killall`、`chmod 777`。
只能作用于模块自身范围；禁止 `iptables -F` 直接清系统所有规则。
只应处理 `mifi_acl`、`mifi_stats`、`MIFI_PROXY`、`MIFI_PROXY_SELF` 等自有链。

### 9. iptables 幂等性
相同规则连续执行 10 次不得重复添加（如 MIFI_PROXY 只能有一个跳转规则）。
`start → start → start` 结果必须等于 `start`。

### 10. 停止后清理
停止代理 / 关闭 MAC 控制 / 停止限速 / 暂停服务后，iptables、tc、进程、临时文件、锁全部正确清理。
禁止页面显示已关闭但旧规则仍存在。

### 11. 锁机制
连续快速点击（热点开/关、保存配置、代理开/关）：不死锁、不永久显示"操作进行中"、超时自动释放、service 重启后旧锁不阻塞。
检查：trap、lock timeout、异常退出清理。

### 12. 进程检查
重启模块后 service.sh=1、httpd=1；禁止 service.sh×4、httpd×3 造成重复执行。

### 13. 错误处理
任何 CGI 失败必须返回 `{"ok": false, "message": "明确错误原因"}`；禁止只返回 `failed/error/-1/undefined`。
示例：`热点当前由系统 Wi-Fi 占用，请先关闭 Wi-Fi 后重试`，而不是 `cmd failed.`

### 14. 测试覆盖要求
自动测试至少覆盖：
- 输入验证：MAC、IP、端口、时间、SSID、密码、流量数值
- 配置：load、save、load-save-load
- MAC：blacklist、whitelist、blacklist→whitelist、whitelist→blacklist
- Proxy：hotspot、self、both
- 状态解析：热点打开、热点关闭、状态读取失败

最低 30 个自动化测试；稳定版目标 50+ PASS、0 FAIL。

### 15. 代码验收判定
通过须满足：Shell Syntax PASS、JavaScript Syntax PASS、自动测试 PASS、配置读写 PASS、路径检查 PASS、iptables 幂等 PASS、锁机制 PASS。
**一票否决**：语法错误 / JS Error / 配置保存丢失 / 防火墙规则残留导致断网 / 后台无法启动 / 无限循环 / service 无限重启 / 热点无法恢复。

---

## 二、UI 验收标准

### 16-17. 页面结构与主导航
主导航固定：首页 / 设备 / 流量 / 科学 / 消息 / 设置。不建议增加一级页面，低频功能全部进 设置/高级。

### 18. 首页验收
用户打开后台 3 秒内能知道：热点开没开、多少设备、用了多少流量、设备有无异常。
首页不得堆大段解释、低频设置、调试信息、高级网络参数。

### 19. 信息层级
数据 > 文字标签 > 状态颜色 > 图标。数据必须成为第一视觉焦点。

### 20. 按钮规范
同层级按钮高度、圆角、左右 padding、字体一致（保存/保存并重启/测试/更新不能一个 36px 一个 48px）。

### 21. 危险按钮
清空、删除、踢出、禁止、关闭全部、恢复默认 → 必须危险状态；必要操作二次确认。
（例：清空白名单后所有热点客户端将无法联网，确定继续？）

### 22. 状态颜色
正常=绿、警告=黄/橙、错误=红、关闭=灰、处理中=蓝/中性。禁止同一个绿色既表示成功又表示开启。

### 23. Loading
所有异步操作必须有 Loading：点击立即反馈、不挡住整页、超时结束、不得永久"检测中"。超时显示"获取失败/重新加载"。

### 24. 状态同步
UI 展示系统真实状态，不是用户刚点了什么。开启热点必须：提交请求 → 等待系统确认 → SoftApState=Enabled → 页面显示已开启。

### 25. 失败回滚
底层失败时 UI 回到真实状态（如隐藏 SSID 失败 → UI 显示"关闭"）。

### 26. 刷新一致性
任何页面操作执行后刷新，状态不得改变（强制验收项目）。

### 27. 亮暗模式
切换无白屏/闪屏/瞬间高亮、有轻微过渡（建议 150–250ms 渐变）、不重新布局、控件状态不丢。

### 28. 移动端输入框
Android Chrome：text/password/number/time/select 不超出卡片、无异常白块、字体一致、点击不放大页面、placeholder 不错位。

### 29. 触摸反馈
手机端不得出现浏览器默认大块矩形 tap highlight；按钮按下只做轻微透明度/缩放。

### 30. 页面缩放
手机浏览器打开不得自动缩放；检查 viewport；input 字号建议 ≥16px 避免自动 zoom。

### 31. 长文本
设备名称、SSID、订阅地址、错误信息超长不撑破卡片（ellipsis 或换行）。

### 32. 空状态
列表为空不得空白卡片：当前没有设备 / 暂无流量记录 / 暂无短信 / 暂无日志 等明确空状态。

### 33. 错误状态
请求失败显示"获取失败/重新加载"；禁止 undefined、NaN、-1%、null 出现在用户页面。

### 34. UI 一票否决
永久 Loading / undefined / NaN / 控件状态与真实状态相反 / 刷新后状态错误 / 页面严重变形 / 手机端输入框溢出 / 关键按钮无法点击 / 深浅色文字看不清 / 弹窗超出屏幕 → 直接不通过。

---

## 三、功能验收标准（真机）

### 35. 验收环境
主设备：Xiaomi 14 / HyperOS 3 / Android 16 / KernelSU
客户端 A：Android 手机
客户端 B：iPhone / iPad / PC 任选一个
（验证热点端真实联网）

### 36. 安装测试
全新安装 → 重启：模块启用、后台自动启动、页面可访问、无旧配置依赖、默认值正确、默认密码符合设计、日志目录自动创建。

### 37. 升级安装
旧版直接覆盖：旧配置保留、新功能默认值正确、服务正常；不得要求删除数据重装（重大版本除外）。

### 38. 卸载
卸载后必须清除：模块运行进程、iptables 自建规则、tc 限速、Web 服务；不得导致手机无法联网。

### 39. 后台访问
热点开 → `192.168.43.1:8080` 正常；热点关 → 本机访问符合当前方案设计。

### 40. 热点开启
点击开启后验证 5 项：系统热点真开启、客户端能扫描、能连接、能访问互联网、后台显示开启。

### 41. 热点关闭
系统热点真关闭、SSID 消失、连接设备断开、页面显示关闭、守护不错误重复开启（Keepalive 按设计判断）。

### 42-43. 系统手动开关热点
后台必须在规定时间内识别并同步（不得一直"获取设备数据中"、不得页面显示开/系统实际关）。

### 44. SSID 修改
系统实际名称、客户端扫描、页面显示、刷新后、重启热点后全部一致。

### 45. 密码修改
8 位/正常密码/特殊字符；新密码能连、旧密码不能连。

### 46. 频段测试
2.4 / 5 / 6 GHz；硬件不支持时 UI 禁用或明确提示，不得点击成功但系统不支持。

### 47. 隐藏 SSID
打开：热点仍工作、普通扫描不可见、手动输入可连接；关闭：恢复广播；系统命令失败自动回滚。

### 48. 热点保活
Keepalive 开启时手动让热点意外关闭 → 守护发现并重新开启；同时区分用户主动关闭（不得用户关掉又马上打开）。

### 49. 定时开关
每天/工作日/周末、开启与关闭时间：到点真实执行一次，不得一分钟内连续重复执行。

### 50. 空闲自动关闭
无设备 5 分钟关闭：无客户端→关闭；有客户端→不关闭；客户端断开后重新计时。

### 51. 在线设备
≥2 设备：MAC/IP/主机名/在线状态/备注实际一致；断开后合理时间变离线。

### 52. 踢出设备
点击踢出 → 客户端真实断开（不是只在页面删除一行）。

### 53. 黑名单
添加→不能联网；移除→恢复联网；热点重启后规则仍符合保存状态。

### 54. 白名单（当前版本重点回归）
开启白名单：白名单设备可联网、非白名单设备无法联网；切回黑名单后原白名单限制立即消失。

### 55. MAC 模式切换
黑→白→黑→白连续 ≥5 次：网络状态始终符合当前模式、无旧 iptables 残留、页面状态正确。

### 56. 设备备注
设置"我的 iPhone"→ 刷新 / 重启 Web / 重启手机仍保存。

### 57. 流量统计
客户端下载数据：今日/本月/设备流量增长，数量级正确、持续增长、不出现负数。

### 58. 流量显示
禁止 -1%、NaN%、Infinity；套餐为 0/空/未配置时有合理空状态。

### 59. 套餐重置
账期切换：本月数据、套餐剩余、提醒状态正确重置；历史记录不消失。

### 60. 通知渠道
PushPlus / 钉钉 / Bark 各自"测试通知"必须真实收到；接口返回成功但手机没收到不算通过。

### 61. 短信转发
真实短信：收到→模块读取→推送，内容含号码/正文/时间；重复短信不得无限重复推送。

### 62. Mihomo 核心
文件存在、ARM64 可执行、`mihomo -v` 正常；核心不存在时页面明确提示"Mihomo 核心不存在 / 校验失败"，而非仅"启动失败"。

### 63. 订阅配置
有效订阅：保存/更新/解析/节点出现；错误订阅：明确失败原因，且不覆盖原来还能用的配置。

### 64. 代理默认状态（当前版本重点）
全新安装必须验证：`PROXY_SCOPE=hotspot`、`PROXY_SELF=0`，页面显示"仅热点设备"。

### 65. 热点代理（仅热点设备）
热点客户端 Google/YouTube 可访问；手机本机保持直连，不得被意外代理。

### 66. 本机代理（仅本机）
手机本机 Google 可访问；热点客户端不经过代理。

### 67. Both
本机→Proxy、热点客户端→Proxy；同时测试百度等国内网站正常。

### 68. 关闭代理
Mihomo 停止、iptables Proxy 链清理、本机恢复正常、热点设备恢复直连；不得遗留"Google 能开但国内不能开"或全部断网。

### 69. 代理反复开关
开/关连续 10 次：无重复规则、无失联、无崩溃、状态正确。

### 70. 测速
点击有 Loading、有超时；成功显示 ms、失败显示失败；禁止一直"检测中"。

### 71. 手机温度
来自真实系统节点；高负载后温度明显变化；异常读取显示 `--`，不得显示 0°C/-273°C。

### 72. 日志
热点开/关、代理开/关、错误、短信在日志中可见；文件不得无限增长。

### 73. 配置导出导入
导出→修改→导入→配置恢复；导入错误文件必须拒绝。

### 74. 服务重启
`service restart`：Web 恢复、配置不丢、热点状态不乱、Proxy 状态符合设计。

### 75. 手机重启（最终强制项目）
真重启 Android 后检查：模块、Web、热点、配置、定时、代理、通知、流量、设备备注。不得只测 service restart。

### 76. 异常网络测试
无 SIM、移动数据关闭、DNS 失败、订阅失效、Mihomo 不存在 → 页面不崩、功能独立失败（Mihomo 坏了不影响热点基础功能）。

### 77. 热点状态接口失败
模拟 status.cgi 失败 → 前端重试并显示异常，不得永久"检测中"。

### 78. 连续操作测试
连续快速开/关/开/关：后台不挂死、锁不释放失败、UI 与系统同步。

### 79. 长时间稳定测试
Beta 发布前连续运行 ≥12 小时；Stable 建议 24–48 小时。观察内存、进程数、日志、流量统计、热点稳定性、Proxy，不得有明显累积问题。

---

## 四、发布验收标准

### 80. 发布前最终 Checklist
**代码**：Shell 语法全过 / JS 语法全过 / 自动测试全 PASS / 配置字段可持久化 / 默认值前后端一致 / 无旧路径 / 无危险全局 iptables 操作 / 无规则残留 / 无死锁
**UI**：Android 手机正常 / 深色正常 / 浅色正常 / 无闪屏 / 无 undefined / 无 NaN / 无永久 Loading / 输入框正常 / 按钮尺寸统一 / 刷新状态不变化
**热点**：开启 / 关闭 / SSID / 密码 / 频段 / 隐藏 SSID / Keepalive / 定时 / 空闲关闭
**设备**：在线设备 / 离线设备 / 踢出 / 黑名单 / 白名单 / 黑白名单切换 / 设备备注
**流量**：今日统计 / 月统计 / 套餐 / 重置 / 超限 / 无负数/NaN
**消息**：SMS / PushPlus / 钉钉 / Bark
**代理**：默认仅热点 / hotspot / self / both / AUTO / FALLBACK / MANUAL / 测速 / 关闭后规则清理
**系统**：温度 / 日志 / 配置导入 / 配置导出 / 服务重启 / 手机重启

### 81. 版本发布判定
- **Alpha**：允许非核心 Bug、UI 未统一，但热点基础功能不得损坏
- **Beta**：P0=0、P1=0、核心流程全部通过；允许 P2 UI 小问题
- **Stable**：P0=0、P1=0、P2 严重问题=0，并完成 24–48h 稳定测试、手机重启测试、升级测试、异常恢复测试

### 82. Bug 分级
- **P0**（立即停止发布）：手机断网 / 热点无法开启 / 后台无法访问 / iptables 破坏系统网络 / 配置完全丢失 / 手机启动异常
- **P1**（Beta 不允许存在）：页面显示开启实际没开 / 白名单逻辑错误 / 本机代理不生效 / 定时不执行 / 短信不推送 / 设置刷新后恢复旧值
- **P2**：间距不统一、文案错误、Loading 动画不好看、卡片轻微错位（可进入下一修订版本）

---

## 五、发布执行流程（配合 main 仓库流程）

每次发版按以下顺序执行，任何一步失败即停止：
1. 按本验收标准执行**代码验收**（可自动化的项全部执行，产出报告）
2. 在真机执行**功能验收**（用户侧操作 + 真机状态确认，产出清单勾选）
3. 代码验收 + 功能验收均通过后，才允许打包与发布
4. 发布动作本身以用户明示「发布到 github」为准；Full / Lite 双包规则不变
